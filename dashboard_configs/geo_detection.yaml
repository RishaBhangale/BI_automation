# ──────────────────────────────────────────────────────────────────────────────
# Auto-generated by discover_dashboard.py
# Generated:  2026-07-23T19:04:18Z
# Dashboard:  Geo Dashboard
# URL:        https://app.powerbi.com/view?r=eyJrIjoiYWM5MWU1NjctMjQ0NS00MDljLTg4MDQtMDYzZGQ1ZWY5NjE0IiwidCI6ImI1YWYyNDUxLWUyMWItNGFhMi1iNGI1LWRjNTkwNzkwOGRkOCJ9
# Pages:      1
# Visuals:    13 (9 KPI cards, 4 charts)
# Slicers:    0
#   SQL Suggestion: DISABLED (run with --db-uri to enable)
#
# ⚡ REVIEW CHECKLIST:
#   1. Verify each visual entry matches what you see on screen
#   2. Review SQL queries (focus on ⚠️ MEDIUM and ❌ UNMATCHED)
#   3. Fill in source_db credentials (or source_excel path)
#   4. Run: pytest tests/dashboard/ --dashboard-config=<this_file>
# ──────────────────────────────────────────────────────────────────────────────

dashboard:
  name: "Geo Dashboard"
  url: "https://app.powerbi.com/view?r=eyJrIjoiYWM5MWU1NjctMjQ0NS00MDljLTg4MDQtMDYzZGQ1ZWY5NjE0IiwidCI6ImI1YWYyNDUxLWUyMWItNGFhMi1iNGI1LWRjNTkwNzkwOGRkOCJ9"
  pages: []  # Single-page report

source_db:
  driver:   ""    # TODO: e.g. "postgresql" or "mssql+pyodbc"
  host:     ""    # TODO
  port:     ""    # TODO
  database: ""    # TODO
  username: ""    # TODO
  password: ""    # TODO: or "${DB_PASSWORD}" 

source_excel:
  filepath:   ""  # TODO: path to Excel export (alternative to DB)
  sheet_name: ""

# ──────────────────────────────────────────────────────────────────────────────
# Auto-discovered KPI Cards (9 found)
# ──────────────────────────────────────────────────────────────────────────────
kpi_validations:

  - visual_title: "Sales"
    page: ""
    sql_query: ""  # TODO: add SQL query
    tolerance: 0.01

  - visual_title: "Profit Margin"
    page: ""
    sql_query: ""  # TODO: add SQL query
    tolerance: 0.01

  - visual_title: "Quantity"
    page: ""
    sql_query: ""  # TODO: add SQL query
    tolerance: 0.01

  - visual_title: "▼3%"
    page: ""
    sql_query: ""  # TODO: add SQL query
    tolerance: 0.01

  # ⚠️  SKIPPED — '$484.2K' looks like a value, not a visual title.
  #     Verify on the dashboard and add manually if needed.

  - visual_title: "▲28%"
    page: ""
    sql_query: ""  # TODO: add SQL query
    tolerance: 0.01

  # ⚠️  SKIPPED — '10.2%' looks like a value, not a visual title.
  #     Verify on the dashboard and add manually if needed.

  - visual_title: "▲5%"
    page: ""
    sql_query: ""  # TODO: add SQL query
    tolerance: 0.01

  # ⚠️  SKIPPED — '7.6K' looks like a value, not a visual title.
  #     Verify on the dashboard and add manually if needed.


# ──────────────────────────────────────────────────────────────────────────────
# Auto-discovered Charts (4 found)
# 
# EXTRACTION METHOD LEGEND:
#   ✅ aria   — Data read from Power BI accessibility aria-labels.
#             Headless-safe. Works in all modes. No UI clicks.
#   ⚠️  sat    — Requires 'Show as a table' UI flow (hover + More Options).
#             Uses the inner [aria-roledescription] div (real px dims).
#             Falls back to SQL direct comparison if UI flow fails.
#   ❓ unk    — Type not classified. aria attempted first, then sat.
# 
# COVERAGE SUMMARY for this dashboard:
#   ✅  4 aria-extractable  — Line chart, Line chart, Line chart, Segment
#   ⚠️   0 show-as-table    — 
#   ❓  0 unknown          — 
# 
# Strategy A = located by title | Strategy B = located by type+index
# ──────────────────────────────────────────────────────────────────────────────
table_validations:

  # ✅ aria Chart: "Returns" — Strategy B (type+index)
  # Data points read via aria-label — headless-safe, no UI clicks needed.
  # Title is empty or body-text. Locating by type+index.
  - visual_title: ""
    visual_type:  "Line chart"
    visual_index: 0
    page: ""
    sql_query: ""  # TODO: add SQL query
    join_keys:
      - ""  # TODO: column(s) to align on
    compare_cols:
      - ""  # TODO: numeric column(s) to compare
    tolerance: 0.01

  # ✅ aria Chart: "Returns" — Strategy B (type+index)
  # Data points read via aria-label — headless-safe, no UI clicks needed.
  # Title is empty or body-text. Locating by type+index.
  - visual_title: ""
    visual_type:  "Line chart"
    visual_index: 1
    page: ""
    sql_query: ""  # TODO: add SQL query
    join_keys:
      - ""  # TODO: column(s) to align on
    compare_cols:
      - ""  # TODO: numeric column(s) to compare
    tolerance: 0.01

  # ✅ aria Chart: "Returns" — Strategy B (type+index)
  # Data points read via aria-label — headless-safe, no UI clicks needed.
  # Title is empty or body-text. Locating by type+index.
  - visual_title: ""
    visual_type:  "Line chart"
    visual_index: 2
    page: ""
    sql_query: ""  # TODO: add SQL query
    join_keys:
      - ""  # TODO: column(s) to align on
    compare_cols:
      - ""  # TODO: numeric column(s) to compare
    tolerance: 0.01

  # ✅ aria Chart: "Segment" — Strategy A (title-based)
  # Data points read via aria-label — headless-safe, no UI clicks needed.
  - visual_title: "Segment"
    visual_type:  ""
    visual_index: null
    page: ""
    sql_query: ""  # TODO: add SQL query
    join_keys:
      - ""  # TODO: column(s) to align on
    compare_cols:
      - ""  # TODO: numeric column(s) to compare
    tolerance: 0.01


# ──────────────────────────────────────────────────────────────────────────────
# Auto-discovered Slicer State (0 slicer(s))
# These assert that the dashboard's default filter state matches
# the context of your SQL WHERE clauses.
# ──────────────────────────────────────────────────────────────────────────────
expected_filters:
  []  # No slicers detected
